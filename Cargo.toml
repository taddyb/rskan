[workspace]
members = ["rskan", "rskan-py"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
authors = ["taddyb"]
repository = "https://github.com/taddyb/rskan"

[profile.release]
opt-level = 3
lto = "thin"
